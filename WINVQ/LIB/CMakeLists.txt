foreach(SOSLIBNAME SOSDBLC SOSDW1CR SOSDW1CS SOSDW1PR SOSDW1PS SOSMBLC)
add_library("WINVQ${SOSLIBNAME}" STATIC IMPORTED GLOBAL)
set_target_properties("WINVQ${SOSLIBNAME}"
  PROPERTIES IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/${SOSLIBNAME}.LIB"
)
endforeach(SOSLIBNAME)
